# Reference

The generated API reference documents the exported types and helpers from `src/index.ts`.

Key areas:

- document and edge/node types
- immutable helpers for adding nodes and edges
- viewport transform helpers for pan and zoom flows
